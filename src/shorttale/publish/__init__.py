# Intentionally empty.
#
# This package is mounted into the publisher container as `publisher`, where
# only service/youtube/login are importable. Keeping this file bare means the
# app-side client module never gets pulled in there.
